scan:
  extensions: [.ts]
  import-patterns:
    - 'require\s*\(\s*[''"]([^''"]+)[''"]'
    - '(?:import|export)\s+.*?\s+from\s+[''"]([^''"]+)[''"]'
  aliases:
    "@/": "src/"
    "~/": "src/"

rules: []
